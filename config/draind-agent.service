[Unit]
Description=draind per-user agent
After=graphical-session.target draind.service
Wants=draind.service
ConditionUser=!@system
ConditionUser=!root

[Service]
Type=simple
ExecStart=/usr/bin/draind-agent
Restart=on-failure
RestartSec=2

[Install]
WantedBy=graphical-session.target
